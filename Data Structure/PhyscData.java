// 클래스는 같은 파일 내에 만들어도 되지만, 컴파일러에서는
// 새로운 클래스 파일을 생성하였음.
public class PhyscData {
    String name;
    int height;
    double vision;
    public PhyscData(String name, int height, double vision) {
        this.name = name;
        this.height = height;
        this.vision = vision;
    }
}